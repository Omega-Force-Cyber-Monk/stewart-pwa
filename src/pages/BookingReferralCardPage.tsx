import { useState } from "react";
import { Check, Copy, Download, ExternalLink, Hand, QrCode, ScanLine, Share2, TrendingUp } from "lucide-react";
import { useGetReferralCardQuery, useGetSetupStateQuery } from "../store/api/Business/business.api";
import { copyToClipboard } from "../utils/clipboard";

function downloadUrl(url: string, filename: string) { const anchor = document.createElement("a"); anchor.href = url; anchor.download = filename; anchor.target = "_blank"; anchor.rel = "noopener noreferrer"; document.body.appendChild(anchor); anchor.click(); anchor.remove(); }

export default function BookingReferralCardPage() {
  const { data: setup } = useGetSetupStateQuery();
  const { data: referral } = useGetReferralCardQuery();
  const [copied, setCopied] = useState(false);
  const url = referral?.data.bookingUrl || referral?.data.websiteUrl || (setup?.data.business.slug ? `https://${setup.data.business.slug}.quittheapp.com` : "");
  const copy = async () => { if (await copyToClipboard(url)) { setCopied(true); window.setTimeout(() => setCopied(false), 1600); } };
  const share = async () => { if (navigator.share) await navigator.share({ title: "Book direct", url }); else await copy(); };
  const checklist = ["Booking page created", "Contact form active", "Notifications enabled", "Print ready card", "Works direction", "Personal website", "Resource & guide share", "Booking notifications enabled"];
  return <div className="space-y-6"><div><h2 className="text-2xl font-bold text-slate-900">Booking &amp; Referral Card</h2><p className="mt-1 text-sm text-slate-500">Set up and manage your direct booking system, referral card, and booking link from one place.</p></div><div className="grid gap-5 lg:grid-cols-3"><section className="rounded-xl bg-white p-5 shadow-sm"><h3 className="mb-4 flex items-center gap-2 font-semibold text-slate-900"><QrCode className="h-5 w-5 text-dashboard-rider" />QR Code</h3>{referral?.data.qrCodeUrl ? <img src={referral.data.qrCodeUrl} alt="Booking QR code" className="mx-auto h-36 w-36" /> : <div className="grid h-36 place-items-center text-sm text-slate-400">QR code unavailable</div>}<button type="button" onClick={() => referral?.data.qrCodeUrl && downloadUrl(referral.data.qrCodeUrl, "booking-qr.png")} className="mt-4 flex w-full items-center justify-center gap-2 rounded-lg bg-dashboard-rider px-4 py-2.5 text-sm font-semibold text-white"><Download className="h-4 w-4" />Download QR Code</button></section><section className="rounded-xl bg-white p-5 shadow-sm"><h3 className="mb-4 flex items-center gap-2 font-semibold text-slate-900"><ExternalLink className="h-5 w-5 text-dashboard-rider" />Booking Link</h3><input readOnly value={url} className="h-11 w-full rounded-lg bg-slate-50 px-3 text-sm text-slate-600" /><div className="mt-4 flex gap-2"><a href={url || "#"} target="_blank" rel="noopener noreferrer" className="flex flex-1 items-center justify-center gap-2 rounded-lg bg-dashboard-rider px-3 py-2 text-sm font-semibold text-white"><ExternalLink className="h-4 w-4" />Open Page</a><button type="button" onClick={copy} className="flex flex-1 items-center justify-center gap-2 rounded-lg border border-dashboard-rider px-3 py-2 text-sm font-semibold text-dashboard-rider"><Copy className="h-4 w-4" />{copied ? "Copied" : "Copy link"}</button></div></section><section className="rounded-xl bg-white p-5 shadow-sm"><h3 className="mb-4 font-semibold text-slate-900">Booking Setup</h3><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-1">{checklist.map((item) => <p key={item} className="flex items-center gap-2 text-sm text-slate-600"><Check className="h-4 w-4 rounded-full bg-green-100 p-0.5 text-green-600" />{item}</p>)}</div></section></div><div className="grid gap-5 rounded-2xl bg-white p-5 shadow-sm lg:grid-cols-[1.05fr_0.95fr]"><div className="rounded-xl bg-slate-100 p-5"><div className="flex min-h-64 flex-col justify-between rounded-xl bg-gradient-to-br from-white to-orange-100 p-6"><div><p className="text-3xl font-bold text-slate-900">Book Direct<br />Next Time</p><p className="mt-3 max-w-48 text-sm text-slate-600">Private airport transportation you can actually afford.</p></div>{referral?.data.qrCodeUrl && <img src={referral.data.qrCodeUrl} alt="QR code" className="h-24 w-24 self-end rounded-lg bg-white p-2" />}<p className="font-semibold text-slate-800">Your Driver. Your Best Price</p></div></div><div><h3 className="text-lg font-semibold text-slate-900">How it Works</h3><div className="mt-4 grid gap-4 sm:grid-cols-2">{[[Hand, "Hand Out", "Hand your referral card to every passenger after a successful ride."], [ScanLine, "Scan the QR Code", "Passengers scan the QR code to access your booking page."], [TrendingUp, "Book Direct", "They schedule future rides directly with you."], [Share2, "Grow Your Business", "Increase repeat customers and referrals."]].map(([Icon, title, text]) => <div key={String(title)} className="rounded-lg border border-slate-100 p-3"><Icon className="h-5 w-5 text-dashboard-rider" /><p className="mt-2 text-sm font-semibold text-slate-900">{String(title)}</p><p className="mt-1 text-xs leading-5 text-slate-500">{String(text)}</p></div>)}</div><div className="mt-5 flex gap-3"><button type="button" onClick={() => referral?.data.printCardUrl && downloadUrl(referral.data.printCardUrl, "print-ready-card.pdf")} className="flex-1 rounded-lg bg-dashboard-rider px-4 py-2.5 text-sm font-semibold text-white"><Download className="mr-2 inline h-4 w-4" />Download Print-Ready Card</button><button type="button" onClick={share} className="flex-1 rounded-lg border border-dashboard-rider px-4 py-2.5 text-sm font-semibold text-dashboard-rider"><Share2 className="mr-2 inline h-4 w-4" />Share your card</button></div><p className="mt-3 text-xs text-slate-400">You can easily download and share your print ready card</p></div></div></div>;
}
